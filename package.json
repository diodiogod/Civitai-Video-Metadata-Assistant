{
  "name": "civitai-video-metadata-assistant",
  "version": "0.6.8",
  "private": true,
  "type": "commonjs",
  "description": "Firefox/Chrome WebExtension for importing local AI video metadata into Civitai uploads.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/metadata.js && node --check src/content.js && node --check popup.js",
    "package": "node scripts/package.js"
  },
  "engines": {
    "node": ">=20"
  }
}
